describe('aliase', () => {
  it('Aliases in Cypress', () => {
    cy.get('@url').should('contain', 'index.html');
  });

  it('Aliases in Cypress', () => {
    cy.get('@url').should('contain', 'index.html');
  });
});
